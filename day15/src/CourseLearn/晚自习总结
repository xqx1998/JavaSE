1.Set集合
    公共的特点：
        无序，没有索引， 不能存储重复值
    1 HashSet集合
        不能存储重复值的条件：
            元素要重写hashCode()和equals()方法
        底层结构：哈希表=数组+链表
    2 LinkedHashSet集合
        特殊：有序：存和取得顺序一致
        底层结构： 链表+哈希表结构
        不能存储重复值的条件：
                    元素要重写hashCode()和equals()方法
    3 TreeSet集合
        特殊：存储的元素默认按照自然排序
        排序的条件：二选一
            1 存储的元素实现Comparable接口，在重写的compareTo方法中定义规则
            2 存储的元素不需要实现任何接口，但是在创建TreeSet集合的时候通过构造传递Comparato比较器对象
        如何定义规则：
            升序：比较器返回值大于0  自己和别人比较就是升序
            降序：比较器返回值小于0  别人跟自己比较就是降序


